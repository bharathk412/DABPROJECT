name: Demo Workflow

on:
  push:
    branches:
      - main
      - feature/**

jobs:
  demo-job:
    name: Demo job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Message 1
        run: echo "This is message 1"
      - name: Message 2
        run: echo "This is message 2"

      
      
